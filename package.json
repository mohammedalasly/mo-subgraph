{
  "name": "mo-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ mohammedalasly/mo-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mohammedalasly/mo-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mohammedalasly/mo-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mohammedalasly/mo-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.0",
    "@graphprotocol/graph-ts": "0.29.1",
    "@protofire/subgraph-toolkit": "^0.1.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
